import styled from "styled-components";

const Wrapper = styled.div`
  position: relative;
  top: 0;
  left: 0;
  height: 100%;
  padding: 10px;
  overflow: hidden;
`;

export default Wrapper;
