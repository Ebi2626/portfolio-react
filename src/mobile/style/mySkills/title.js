import styled from "styled-components";

const SkillTitle = styled.h2`
  font-size: 18px;
  text-align: center;
  & > em {
    font-size: 14px;
    font-weight: normal;
    color: #aaa;
  }
`;

export default SkillTitle;
